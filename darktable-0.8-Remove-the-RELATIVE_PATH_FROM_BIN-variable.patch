diff --git a/CMakeLists.txt b/CMakeLists.txt
index c252af6..68ee0d3 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -70,12 +70,6 @@ if(NOT CMAKE_BUILD_TYPE)
 endif(NOT CMAKE_BUILD_TYPE)
 
 if(WIN32)
-	set(RELATIVE_PATH_FROM_BIN ${CMAKE_INSTALL_PREFIX} CACHE STRING "Relative path to library from binary")
-else(WIN32)
-	set(RELATIVE_PATH_FROM_BIN ${CMAKE_INSTALL_PREFIX} CACHE STRING "Relative path to library from binary")
-endif(WIN32)
-
-if(WIN32)
 	set(SHARE_INSTALL "." CACHE STRING "Data file install path. Must be a relative path (from CMAKE_INSTALL_PREFIX), with no trailing slash.")
 else(WIN32)
 	if(NOT SHARE_INSTALL)
diff --git a/src/config.cmake.h b/src/config.cmake.h
index e2e4cd5..8343a86 100644
--- a/src/config.cmake.h
+++ b/src/config.cmake.h
@@ -12,8 +12,8 @@
 #define PACKAGE_STRING PACKAGE_NAME " " PACKAGE_VERSION
 #define PACKAGE_BUGREPORT "hanatos@gmail.com"
 
-#define DARKTABLE_LIBDIR "@RELATIVE_PATH_FROM_BIN@/@LIB_INSTALL@"
-#define DARKTABLE_DATADIR "@RELATIVE_PATH_FROM_BIN@/@SHARE_INSTALL@"
+#define DARKTABLE_LIBDIR "@CMAKE_INSTALL_PREFIX@/@LIB_INSTALL@"
+#define DARKTABLE_DATADIR "@CMAKE_INSTALL_PREFIX@/@SHARE_INSTALL@"
 
 
 #endif
-- 
1.7.4


